version: 0.2

phases:
  install:
   runtime-versions:
    java: corretto8
   commands:
     - echo Entering install phase...
     - echo Nothing to do now
  build:
    commands:
     - echo Build started on `date`
     - ./initial/gradlew build
     - echo `pwd`
     - echo `ls`
artifacts:
  type: zip
  files:
    - './build/libs/gs-spring-boot-0.1.0.jar'